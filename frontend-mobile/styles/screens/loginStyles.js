export const loginStyles = (theme) => ({
  formContainer: {
    backgroundColor: theme.card,
    borderRadius: 14,
    padding: 18,
    width: "100%",
    shadowColor: "#000",
    shadowOpacity: 0.15,
    shadowRadius: 7,
    elevation: 4,
  },
  link: {
    marginTop: 12,
    color: theme.primary,
    fontWeight: "600",
    textAlign: "center",
  },
});
